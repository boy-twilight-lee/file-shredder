import type { ButtonProps } from '@arco-design/web-vue';

// 气泡内确认浮层统一使用中等按钮，保持鼠标操作区域一致。
export const MEDIUM_POPCONFIRM_CANCEL_BUTTON_PROPS: ButtonProps = {
  size: 'medium',
  type: 'outline',
};

export const MEDIUM_POPCONFIRM_PRIMARY_BUTTON_PROPS: ButtonProps = {
  size: 'medium',
  type: 'primary',
};
